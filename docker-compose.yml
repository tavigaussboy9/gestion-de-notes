version: '3.8'
services:
  auth:
    build:
      context: ./service-authentification
    ports:
      - '5000:5000'
    restart: always
  notes:
    build:
      context: ./service-notes
    ports:
      - '5001:5001'
    restart: always
  notifications:
    build:
      context: ./service-notifications
    ports:
      - '5002:5002'
    restart: always